import { AnimatedListDirective } from './animated-list.directive';

describe('AnimatedListDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimatedListDirective({} as any, {} as any);
    expect(directive).toBeTruthy();
  });
});
