import { DataToViewListPipe } from './data-to-view-list.pipe';

describe('DataToViewListPipe', () => {
  it('create an instance', () => {
    const pipe = new DataToViewListPipe();
    expect(pipe).toBeTruthy();
  });
});
